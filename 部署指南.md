# 星星书架 · 完整版使用与部署指南

前后端一体（纯 Node，零依赖），包含：登录、孩子账号打卡、家长端看板、暑假倒计时。

---

## 一、本地运行（最快，立即可用）

```bash
cd app
node server.js
```

启动后访问 **http://127.0.0.1:3000**（同一 Wi-Fi 下，手机可用电脑局域网 IP 访问，例如 http://192.168.x.x:3000）。

**演示账号**
| 角色 | 账号 | 密码 | 登录后进入 |
|---|---|---|---|
| 家长 | dad | 123456 | 家长端看板（看小明、小红） |
| 孩子 | xiaoming | 123456 | 孩子端（打卡/书架/储蓄罐/倒计时） |
| 孩子 | xiaohong | 123456 | 孩子端 |

数据存在 `app/data.json`（首次启动自动生成），删了它即可重置回演示数据。

---

## 二、部署到轻量应用服务器（推荐，国内稳）

> 适合有公网 IP 的云服务器（腾讯云 / 阿里云 轻量应用服务器）。花钱但最稳、数据不丢、国内访问快。代码已推到 GitHub，下面用「本机打包上传」方式（不依赖服务器访问 GitHub，最稳）。

### 第 1 步：买一台轻量服务器
- 腾讯云轻量应用服务器：https://cloud.tencent.com/product/lighthouse （选 **Ubuntu 22.04**，2 核 2G / 1 核 2G 都够，带宽 3–6 Mbps，约 ¥60–100/年）
- 阿里云轻量应用服务器：https://www.aliyun.com/product/swas （同样选 Ubuntu 22.04）
- 创建时设置 **root 密码**（记住它），并在控制台「防火墙 / 安全组」放行 **TCP 3000** 端口（入站规则）。

### 第 2 步：SSH 登录服务器
在本机终端（Git Bash / PowerShell 均可）：
```bash
ssh root@你的服务器IP
```

### 第 3 步：本机打包代码并上传（最稳，不依赖服务器访问 GitHub）
在本机终端（不是服务器）执行：
```bash
cd app
zip -r star-shelf.zip . -x data.json
scp star-shelf.zip root@你的服务器IP:/opt/star-shelf.zip
```
然后在服务器上解压：
```bash
mkdir -p /opt/star-shelf && cd /opt/star-shelf && unzip -o /opt/star-shelf.zip
```

### 第 4 步：一键装环境并启动
在服务器上执行（脚本自动装 Node 18、装依赖、用 pm2 守护，重启不丢）：
```bash
cd /opt/star-shelf && bash deploy.sh
```

### 第 5 步：访问
浏览器打开 `http://你的服务器IP:3000` 即可。演示账号见文末。
数据存在服务器 `/opt/star-shelf/data.json`，重启不丢。
> 以后更新代码：本机重新 zip + scp 覆盖，再 `cd /opt/star-shelf && bash deploy.sh` 即可。

---

## 三、部署到 Render（国外，国内可能打不开）

> 前提：需要一个代码仓库账号（GitHub 或 GitLab 均可，免费）。Render 通过连接 Git 仓库来自动部署。

### 第 1 步：注册一个 GitHub 账号（本人操作，约 2 分钟）
1. 打开 https://github.com/signup → 填邮箱、设密码、起用户名 → 验证邮箱。
2. 按提示完成（可能要手机号验证 / 两步验证）。
3. 记住用户名和密码即可，后面只用网页操作。

> 不想用 GitHub？GitLab 同样免费可用：https://gitlab.com/users/sign_up

### 第 2 步：在 GitHub 新建一个空仓库
1. 登录 GitHub → 右上角 **＋ → New repository**。
2. 仓库名随便起（例如 `star-shelf`），**不要**勾选 Add README / .gitignore / License（保持空仓库）。
3. 点 **Create repository**，进入后复制仓库地址（HTTPS，形如 `https://github.com/你的名/star-shelf.git`）。

### 第 3 步：把本地代码推上去（在本机终端执行）
项目目录 `app/` 里已经 `git init` 并做好了初次提交，你只需关联远程并推送：

```bash
cd app
git remote add origin https://github.com/你的名/star-shelf.git
git branch -M main
git push -u origin main
# 提示登录时，用 GitHub 网页里生成的 Personal Access Token（不是账号密码）作为密码
# 生成地址：GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic) → Generate new token (勾 repo)
```

### 第 4 步：在 Render 一键部署
1. 打开 https://render.com → 用 GitHub 登录授权。
2. **New → Blueprint**（不是 Web Service）→ 选刚才的 `star-shelf` 仓库 → Connect。
3. Render 会自动读取仓库里的 `render.yaml`，直接点 **Apply**。
4. 部署完成（约 1–2 分钟）即得到公网网址，形如 `https://star-shelf.onrender.com`。

> 也可以手动建：New → Web Service → 选仓库 → Build Command 留空、Start Command 填 `node server.js` → Create。

---

## 四、说明与注意事项
- **数据持久化**：当前用 `data.json` 文件存储。Render 免费层的文件系统是临时的，服务重启/休眠后数据可能被重置回演示数据。正式长期用建议把存储换成数据库（Supabase / MongoDB / MySQL），数据模型已在《作业管理系统数据结构设计》里给好。
- **休眠**：免费 Web Service 一段时间没人访问会自动休眠，下次访问会冷启动（首屏慢几秒），属正常现象。
- **改账号/加孩子**：直接改 `server.js` 里的 `seed()` 后重新部署；或后续我帮你加一个「家长添加孩子」的自助入口。

---

## 五、备选：Railway / Fly.io
同样流程：连接仓库 → Start Command `node server.js` → 自动分配域名。Railway 对新项目有免费额度。
